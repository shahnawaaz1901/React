/*
    useState : useState is the function which provides us stateful logic inside 
            the functional component. We need to first create a state inside the 
            functional Component, Syntax for useState function is 

    const [stateName, setStateFunctionName] = useState(InitialValueOfState)

    Here stateName is the similar to this.state and setStateFunctionName is similar 
            to this.setState which we used to set the State or update the Current
            State
*/
